{
  "name": "dev-templates",
  "version": "1.0.0",
  "private": true,
  "author": {
    "name": "Notum Technologies",
    "email": "info@notum.cz",
    "url": "https://notum.cz"
  },
  "maintainers": [
    {
      "name": "Notum Technologies",
      "email": "info@notum.cz",
      "url": "https://notum.cz"
    }
  ],
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npx turbo build --cache-dir=.turbo --parallel",
    "build:fast": "NEXT_SHARP=0 GENERATE_SOURCEMAP=false npx turbo build --cache-dir=.turbo --parallel --concurrency=12",
    "build:optimized": "NODE_ENV=production NEXT_SHARP=0 GENERATE_SOURCEMAP=false NEXT_TELEMETRY_DISABLED=1 npx turbo build --cache-dir=.turbo --parallel --concurrency=12 --force",
    "build:production": "NODE_ENV=production NEXT_SHARP=0 GENERATE_SOURCEMAP=false npx turbo build --cache-dir=.turbo --parallel --concurrency=12",
    "build:strapi": "npx turbo run build --filter=@repo/strapi --cache-dir=.turbo",
    "build:ui": "npx turbo run build --filter=@repo/ui --cache-dir=.turbo",
    "clean": "npx turbo clean && rm -rf .turbo && rm -rf node_modules/.cache",
    "clean:all": "rm -rf node_modules && rm -rf .turbo && rm -rf apps/*/node_modules && rm -rf apps/*/.next && rm -rf apps/*/dist",
    "commit": "git-cz",
    "dev": "npx turbo dev",
    "dev:strapi": "npx turbo run dev --filter=@repo/strapi",
    "dev:ui": "npx turbo run dev --filter=@repo/ui",
    "dev:unified": "docker-compose -f docker-compose.dev.yml up --build",
    "docker:build": "DOCKER_BUILDKIT=1 docker-compose -f docker-compose.multi-site.yml build --parallel --compress",
    "docker:build:fast": "DOCKER_BUILDKIT=1 docker-compose -f docker-compose.multi-site.yml build --parallel --compress --pull",
    "docker:build:optimized": "DOCKER_BUILDKIT=1 BUILDKIT_INLINE_CACHE=1 docker-compose -f docker-compose.multi-site.yml build --parallel --compress --pull --build-arg BUILDKIT_INLINE_CACHE=1",
    "docker:clean": "docker system prune -f && docker builder prune -f",
    "format": "prettier --write --cache --ignore-unknown \"**/*.{js,jsx,ts,tsx,md,css,scss}\"",
    "format:check": "prettier --check --cache --ignore-unknown \"**/*.{js,jsx,ts,tsx,md,css,scss}\"",
    "heroku-postbuild": "bash ./scripts/heroku/heroku-postbuild.sh",
    "preinstall": "npx only-allow yarn",
    "postinstall": "node -e \"try { require('sharp') } catch (e) { console.log('Sharp not found, skipping...') }\"",
    "lint": "npx turbo lint --cache-dir=.turbo --parallel",
    "lint:fix": "npx turbo lint:fix --cache-dir=.turbo --parallel",
    "prepare": "husky",
    "preview": "npx turbo run build:fast && npx turbo run start",
    "quality": "yarn lint:fix && yarn format",
    "quality:check": "yarn lint && yarn format:check",
    "start": "npx turbo run start",
    "test": "npx turbo run test --cache-dir=.turbo --parallel",
    "typecheck": "npx turbo typecheck --cache-dir=.turbo --parallel"
  },
  "prettier": "@repo/prettier-config",
  "dependencies": {
    "sharp": "^0.34.2"
  },
  "devDependencies": {
    "@commitlint/cli": "19.3.0",
    "@commitlint/config-conventional": "19.2.2",
    "@repo/eslint-config": "*",
    "@repo/prettier-config": "*",
    "@repo/typescript-config": "*",
    "commitizen": "4.3.0",
    "cz-conventional-changelog": "^3.3.0",
    "eslint": "^8.57.0",
    "husky": "^9.1.1",
    "lint-staged": "15.2.7",
    "prettier": "3.3.2",
    "turbo": "^2.0.9"
  },
  "packageManager": "yarn@1.22.22",
  "engines": {
    "node": ">=20.x.x",
    "yarn": "1.22.x"
  },
  "comments": {
    "prettier": "bump to 3.3.3 destroys types schemas generated by Strapi (@strapi/typescript-utils > generators > utils > prettier.format())"
  }
}
