# UI App Local Development Environment Variables
# Copy this file to .env.local and fill in your actual values
# For production deployment, use the unified environment approach via scripts/deploy/env-generator.sh

# Development URLs (change these if running Strapi on different ports)
NEXT_PUBLIC_APP_PUBLIC_URL=http://localhost:3000
NEXT_PUBLIC_STRAPI_URL=http://localhost:1337

# NextAuth (generate your own secret with: openssl rand -base64 32)
NEXTAUTH_SECRET=your-nextauth-secret-key-here
NEXTAUTH_URL=http://localhost:3000

# NextJS settings
NEXT_OUTPUT=
NODE_ENV=development
NEXT_IMAGES_UNOPTIMIZED=false

# Optional: Sentry (leave empty to disable)
NEXT_PUBLIC_SENTRY_DSN=
